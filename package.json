{
  "name": "worldcup2026sweepstake",
  "version": "1.0.0",
  "description": "World Cup 2026 sweepstake",
  "main": "index.html",
  "scripts": {
    "watch:sass": "sass --no-source-map --watch src/scss:public/css",
    "watch:eleventy": "eleventy --serve --port=8081",
    "build:sass": "sass --no-source-map src/scss:public/css",
    "build:eleventy": "eleventy",
    "start": "npm-run-all --parallel watch:*",
    "build": "npm-run-all build:sass build:eleventy",
    "clean": "rimraf ./public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robjwood/wc-2026.git"
  },
  "author": "Rob",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.66.1"
  },
  "dependencies": {
    "@11ty/eleventy-fetch": "^4.0.1",
    "dotenv": "^16.4.5",
    "netlify-plugin-cache": "^1.0.3"
  }
}
